<ul class="combined-list">
<% 
	# TODO: Need to fine tune here.
	#observe :question, 
    #      :my_collection_item, 
    #      :my_collection, 
    #      :recommendation, TODO: received_recommendation is not yet DONE.
    #      :my_collection_comment,
    #      :my_collection_response
	#  	   :product
  
	activities.each do |activity| 
		if activity.name == 'Question'
%>
			<%= render :partial => 'activities/question', :locals => { :activity => activity } %>
<%
		elsif activity.name == 'MyCollectionItem'
%>
			<%= render :partial => 'activities/my_collection_item', :locals => { :activity => activity } %>
<%			
		elsif activity.name == 'MyCollection'
%>
			<%= render :partial => 'activities/my_collection', :locals => { :activity => activity } %>
<%			
		elsif activity.name == 'Recommendation'
%>
			<%= render :partial => 'activities/product_recommendation', :locals => { :activity => activity } %>
<%			
		elsif activity.name == 'Product'
%>
			<%= render :partial => 'activities/product', :locals => { :activity => activity } %>
<%
		end
%>
<% end %>
</ul>