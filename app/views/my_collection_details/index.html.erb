<h1>Listing my_collection_details</h1>

<table>
  <tr>
  </tr>

<% @my_collection_details.each do |my_collection_detail| %>
  <tr>
    <td><%= link_to 'Show', my_collection_detail %></td>
    <td><%= link_to 'Edit', edit_my_collection_detail_path(my_collection_detail) %></td>
    <td><%= link_to 'Destroy', my_collection_detail, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New my_collection_detail', new_my_collection_detail_path %>