$("#response-stat-product-<%= @product.id %>").html("<%= @number_of_likes %>");
try {$("#response-product-<%= @product.id %>").html('<%= link_to "Unlike", unlike_product_path(@product, :user_id => current_user.id), :class => "button-green checked", :id => "own-this", :title => "Unlike", :remote => true %>');} catch (e) {}
try {$("#home-response-product-<%= @product.id %>").html('<%= link_to "Unlike", unlike_product_path(@product, :user_id => current_user.id), :title => "Unlike", :remote => true %>');} catch (e) {}
