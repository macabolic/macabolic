<%= form_for(@my_collection_item) do |f| %>
  <% if @my_collection_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_collection_item.errors.count, "error") %> prohibited this my_collection_item from being saved:</h2>

      <ul>
      <% @my_collection_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :my_collection_id %><br />
    <%= f.text_field :my_collection_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
