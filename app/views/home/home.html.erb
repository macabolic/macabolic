<style type="text/css">
#backgroundZoomIn {
	display:none;
	position:fixed;
	_position:absolute; /* hack for internet explorer 6*/
	height:100%;
	width:100%;
	top:0;
	left:0;
	background:#000000;
	border:1px solid #cecece;
	z-index:1;
}
#popupZoomIn {
	display:none;
	position:fixed;
	_position:absolute; /* hack for internet explorer 6*/
	//background:#f6f1de;
	background: #DA5D18;
	//border:2px solid #cecece;
	z-index:2;
	padding:12px;
	font-size:13px;
	width: 400px; 
	//height: 300px; 	
  	-webkit-border-radius:20px;
  	-moz-border-radius:20px;
  	border-radius:20px;		
	font-color: #ffffff;
	color: #ffffff;
}

#popupZoomInClose {
	font-size:14px;
	line-height:14px;
	right:3px;
	top:1px;
	position:absolute;
	color:#5c5c5c;
	font-weight:700;
	display:block;
}

#product_preview_gallery {
	cursor: pointer;
}

#zoom_now {
	color: #595959;
	cursor: pointer;
	text-decoration: underline;
}
</style>
<script type="text/javascript"> 
    $(document).ready(function(){ 
        $(document).pngFix(); 
//		$("ul.tabs li:first").addClass("active").show(); //Activate first tab
    }); 
</script>
<script type="text/javascript"> 
//SETTING UP OUR POPUP
//0 means disabled; 1 means enabled;
var zoomInStatus = 0;

//loading popup with jQuery magic!
function loadZoomIn(){
	//loads popup only if it is disabled
	if(zoomInStatus==0){
		$("#backgroundZoomIn").css( {"opacity": "0.7"} );
		$("#backgroundZoomIn").fadeIn("slow");
		$("#popupZoomIn").fadeIn("slow");
		zoomInStatus = 1;
	}
}

//disabling popup with jQuery magic!
function disableZoomIn(){
	//disables popup only if it is enabled
	if(zoomInStatus==1){
		$("#backgroundZoomIn").fadeOut("slow");
		$("#popupZoomIn").fadeOut("slow");
		zoomInStatus = 0;
	}
}

//centering popup
function centerZoomIn(){

	//request data for centering
	var windowWidth = document.documentElement.clientWidth;
	var windowHeight = document.documentElement.clientHeight;
	var popupHeight = $("#popupZoomIn").height();
	var popupWidth = $("#popupZoomIn").width();
	//centering
	$("#popupZoomIn").css({
		"position": "absolute",
		"top": windowHeight/2-popupHeight/2,
		"left": windowWidth/2-popupWidth/2
	});
	//only need force for IE6

	$("#backgroundZoomIn").css( {"height": windowHeight} );
}


$(document).ready(function() {
 	$(document).pngFix(); 

	//LOADING POPUP
	//Click the button event!
	$("#product_preview_gallery").click(function(){
		//centering with css
		centerZoomIn();
		//load popup
		loadZoomIn();
	});

	$("#zoom_now").click(function() {
		centerZoomIn();
		loadZoomIn();
	});
	
	//CLOSING POPUP
	//Click the x event!
	$("#popupZoomInClose").click(function(){
		disableZoomIn();
	});
	
	//Click out event!
	$("#backgroundZoomIn").click(function(){
		disableZoomIn();
	});
	
	//Press Escape event!
	$(document).keypress(function(e){
		if(e.keyCode==27 && zoomInStatus==1) {
			disableZoomIn();
		}
	});	
	
});
</script>
<div id="popupZoomIn">
	<%= render :template => 'home/what_we_do' %>
</div>
<div id="backgroundZoomIn"></div>

<% title "Home" %>
	<div id="grey-strip" style="height: 115px">
		<div class="wrapper-top">
			<%= render 'layouts/tagline' %>
		</div>
	</div>
	<!-- ====================================================================================================== -->
	<div class="wrapper-mh">
		<% if !user_signed_in? %>
		<div>
			<%= render :partial => "home/learn_more" %>	
		</div>
		<% end %>
		
		<div class="maincol">
	        <%= render :partial => 'searches/form' %>
			<div class="tabbed-content-area">				
				
		        <div id="tab1" class="tab_content product-grid group"> 
					<% @user = User.new %>
					<%= render :partial => 'products/index', :locals => { :products => @featured_products } %>
				</div>	
			</div>
			<br/>
					
			<div class="tabbed-content-area">	
				<div style="margin-top: 15px; font-size: 20px;padding-top:5px; text-align: center">
					<span class="home-signupbutton">
				   		Macabolic recently ...
					</span>
				</div>									
				<br/>
				<% @feature_collection_items.each do |collection_items| %>
				<% if collection_items.size > 0 %>
				<div class="pagination-wrapper top">
					<div>
						<p style="font-size:14px; color:#808080; padding-left: 23px; text-align:left">
							<%= image_tag current_profile_image_thumbnail_url(collection_items[0].user), :width => 50, :title => collection_items[0].user.full_name %>
							<strong><span><%= collection_items[0].user.full_name %></span></strong>
						</p>
					</div>
				</div><!-- .pagination-wrapper -->						
		        <div class="product-grid group"> 
					<%= render :partial => 'my_collection_items/index', :locals => { :my_collection_items => collection_items, :mode => "read", :filter_my_collection_item => nil } %>
				</div>
				<br/>	
				<% end %>
				<% end %>
			</div>
		<!--				
			<div class="tabbed-content-area">
				<ul class="tabs"> 
			        <li><a href="#tab1">Featured Collections</a></li> 
				</ul> 		
		        <div id="tab1" class="tab_content product-grid group"> 
					<%#= render :partial => 'my_collections/index', :locals => { :my_collections => MyCollection.all(:limit => 6), :preview_gallery => false } %>
				</div>	
			</div>
		-->
		</div>
		
		<% if @notice.present? %>		
		<div class="sidebar">		
			<div class="sidebar-content" id="recent-search">
			    <div class="sidebar-header">Thank you for your interest!</div>
				<div class="sidebar-inner">
					<p style="background:#fff; color: red; border: 2px dotted red; padding: 4px;">
						We are on invitation now. We will let you know when it is ready.
					</p>
				</div>
			</div>
		</div>
		<% end %>
			
		<% if !user_signed_in? %>
		<div class="sidebar">
			<%= render :partial => 'users/sessions/mini_form' %>
		</div>
		<% else %>
			<%#= render 'products/add_a_product_link' %>		
		<!--
			<div class="sidebar-content account-info">
				<div class="sidebar-inner">					
			    	<p class="username">
						<%#= image_tag current_profile_image_thumbnail_url(current_user), :width => 50, :height => 50 %>
			    		<span><%#= current_user.full_name %></span>
			    	</p>
				</div>
				<div style="height: 300px; margin: 5px 5px 5px;">
				<%# product_lines = ProductLine.all %>
					<ul>
						<%# product_lines.each do |product_line| %>
						<li><%#= link_to product_line.name, find_product_searches_path(:query => product_line.name) %></li>
						<%# end %>
					</ul>
				</div>
			</div>
		-->
		
		<% end %>		

		<%= render :template => 'home/heat_map' %>
		
	<!--	
		<div class="sidebar">
			<div class="sidebar-content">
	            <div class="sidebar-header" style="background: #dd6219;">Did you know?</div>			
				<div style="margin: 17px; border: 0px;">
					<h3 style="text-align:left;">There are always something EXTRA!</h3><br/>
					<div>
						<%#= image_tag "/images/featuretour/extra.png" %>
						<br/><br/>
						<p style="font-size: 12px; font-family: Verdana;">
							There is an easy way to tag the product you like while you are visiting another website.
							<br/><br/>
							Check it out at our <%#= link_to 'Extra', extra_path %> for the widget.
							<br/>					
						</p>
					</div>
				</div>
			</div>
		</div>
	-->	
	</div>	
