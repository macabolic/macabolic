<table>
  <tr>
    <th>Name</th>
    <th>Vendor</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_lines.each do |product_line| %>
  <tr>
    <td><%= product_line.name %></td>
    <td><%= product_line.vendor.name %></td>
    <td><%= link_to 'Show', [:admin, product_line] %></td>
    <td><%= link_to 'Edit', edit_admin_product_line_path(product_line) %></td>
    <td><%= link_to 'Destroy', [:admin, product_line], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product line', new_admin_product_line_path %>
