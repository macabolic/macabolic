<%= form_for([:admin, @product_line]) do |f| %>
  <% if @product_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_line.errors.count, "error") %> prohibited this product_line from being saved:</h2>

      <ul>
      <% @product_line.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :vendor_id %><br />
	<%= select_tag "product_line[vendor_id]", options_from_collection_for_select(Vendor.all, "id", "name") %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
