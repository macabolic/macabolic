<%= semantic_form_for [:admin, @product], :html => { :multipart => true } do |f| %>
	<%= f.inputs "Product" do %>
		<%= f.input :name %>
		<%= f.input :vendor %>
		<%= f.input :product_line %>
		<%= f.input :thumbnail %>
		<%= f.input :description %>
	<% end %>
	<%= f.buttons %>
<% end %>

