$("#response-stat-my-collection-<%= @my_collection.id %>").html("<%= @number_of_likes %>");
try {$("#response-my-collection-<%= @my_collection.id %>").html('<%= link_to "Unlike", unlike_my_collection_path(@my_collection, :user_id => current_user.id), :class => "button-green checked", :id => "own-this", :title => "Unlike", :remote => true %>');} catch (e) {}
try {$("#home-response-my-collection-<%= @my_collection.id %>").html('<%= link_to "Unlike", unlike_my_collection_path(@my_collection, :user_id => current_user.id), :title => "Unlike", :remote => true %>');} catch (e) {}
