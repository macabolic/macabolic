$("#follower-stat-my-collection-<%= @my_collection.id %>").html("<%= @number_of_followers %>");
try {	$("#follow-my-collection-<%= @my_collection.id %>").html('<%= link_to "Follow", follow_my_collection_path(@my_collection, :follower_id => current_user.id), :class => "button-green", :id => "own-this", :title => "Follow", :remote => true %>'); } catch (e) {}
try {	$("#home-follow-my-collection-<%= @my_collection.id %>").html('<%= link_to "Follow", follow_my_collection_path(@my_collection, :follower_id => current_user.id), :title => "Follow", :remote => true %>'); } catch (e) {}
