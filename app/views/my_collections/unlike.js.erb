$("#response-stat-my-collection-<%= @my_collection.id %>").html("<%= @number_of_likes %>");
try {$("#response-my-collection-<%= @my_collection.id %>").html('<%= link_to "Like", like_my_collection_path(@my_collection, :user_id => current_user.id), :class => "button-green", :id => "own-this", :title => "Like", :remote => true %>');} catch (e) {}
try {$("#home-response-my-collection-<%= @my_collection.id %>").html('<%= link_to "Like", like_my_collection_path(@my_collection, :user_id => current_user.id), :title => "Like", :remote => true %>');} catch (e) {}
