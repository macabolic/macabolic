$("#follower-stat-my-collection-<%= @my_collection.id %>").html("<%= @number_of_followers %>");
try { $("#follow-my-collection-<%= @my_collection.id %>").html('<%= link_to "Following", unfollow_my_collection_path(@my_collection, :follower_id => current_user.id), :class => "button-green checked", :id => "own-this", :title => "Unfollow", :remote => true %>'); } catch (e) {}
try { $("#home-follow-my-collection-<%= @my_collection.id %>").html('<%= link_to "Unfollow", unfollow_my_collection_path(@my_collection, :follower_id => current_user.id), :title => "Unfollow", :remote => true %>'); } catch (e) {}