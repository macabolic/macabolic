$("#follower-stat-store-<%= @vendor.id %>").html("<%= @number_of_followers %>");
try {	$("#follow-store-<%= @vendor.id %>").html('<%= link_to "Follow", follow_store_path(@vendor, :follower_id => current_user.id), :class => "button-green", :id => "own-this", :title => "Follow", :remote => true %>'); } catch (e) {}
