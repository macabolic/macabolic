$("#response-stat-store-<%= @vendor.id %>").html("<%= @number_of_likes %>");
try {$("#response-store-<%= @vendor.id %>").html('<%= link_to "Unlike", unlike_store_path(@vendor, :user_id => current_user.id), :class => "button-green checked", :id => "own-this", :title => "Unlike", :remote => true %>');} catch (e) {}
