<%= form_for(@photo_entry) do |f| %>
  <% if @photo_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_entry.errors.count, "error") %> prohibited this photo_entry from being saved:</h2>

      <ul>
      <% @photo_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
